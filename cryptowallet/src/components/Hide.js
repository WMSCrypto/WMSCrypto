import React from 'react';
import PropTypes from 'prop-types';

class Hide extends React.Component {
    render() {

    }
}

Hide.defaultProps = {
    hide: false
};

Hide.propTypes = {
    hide: PropTypes.bool
};

export default Hide;